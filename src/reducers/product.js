"use strict"

import {
} from '../constants/productType';

const INITIAL_STATE = {
  search: ""
};

export function productsReducers(state = INITIAL_STATE, action) {
  switch (action.type) {
  }
  return state
}