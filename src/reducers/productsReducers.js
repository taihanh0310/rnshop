"use strict"

// import {
// } from '../constants/productType';

const INITIAL_STATE = {
  search: "",
  products: [],
  product: {}
};

export default function productsReducers(state = INITIAL_STATE, action) {
  switch (action.type) {
  }
  return state
}