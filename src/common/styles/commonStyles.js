import {
  Dimensions,
  StyleSheet,
} from 'react-native'

const commonStyles = StyleSheet.create({
  defaultMargin: {
    margin: 10
  },
  defautPadding: {
    padding: 10
  }
})

export default commonStyles